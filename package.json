{
  "name": "show-of-force",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Show of Force — a first-person tactical stepper betting game (PWA)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^5.4.0"
  }
}
