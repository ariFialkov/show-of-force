{
  "name": "show-of-force",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Show of Force — a first-person tactical stepper betting game (PWA)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs",
    "bake:trooper": "node scripts/bake-trooper.mjs",
    "bake:props": "node scripts/bake-props.mjs"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "meshoptimizer": "^1.2.0",
    "playwright-core": "^1.62.1",
    "vite": "^5.4.0"
  }
}
